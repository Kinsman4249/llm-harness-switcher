todo.md
handoff.md
$HOME/

# bench/ - per-run debug logs and query_and_grade.py's generated grading
# output are transient, not something future sessions need committed
bench/logs/
bench/*.result.*.json

# Python bytecode caches are regenerable artifacts, never source
__pycache__/
*.pyc

# Kilo's own workspace (agent Manager / plans / node_modules), not released
/.kilo/
