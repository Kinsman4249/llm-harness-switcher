todo.md
handoff.md
$HOME/

# bench/ - per-run debug logs, haystack prompt builds, and query_and_grade.py's
# generated grading output are transient, not something future sessions need
# committed
bench/logs/
bench/haystack*.json
bench/*.result.*.json
bench/coding_challenge_*.run.json
bench/coding_challenge_*.result.json
# coding-challenge snapshot is a transient hermetic copy of the repo tree
bench/_challenge_repo/

# Python bytecode caches are regenerable artifacts, never source
__pycache__/
*.pyc

# Kilo's own workspace (agent Manager / plans / node_modules), not released
/.kilo/
